# 🗂️ Extract File Names

This project is a simple and efficient Python algorithm that reads all files located in the folder `extract_name_from_file/utils/files_to_be_searched` and returns their names **without the file extensions**.

## 💡 Example Output

Suppose the folder `files_to_be_searched` contains the following files:

```
doc.pdf
text.txt
```

The program output will be:

```
doc
text
```

## 📄 About the Challenge

You can find the original challenge description in the [`chalenge.txt`](extract_name_from_file/chalenge.txt) file.

## 🛠️ Technologies Used

- Language: **Python**
- Libraries: Only **native modules**
- Virtual Environment: Configured with **venv**

## ▶️ How to Run

1. **Clone the repository** (if you haven't already):
   ```bash
   git clone https://github.com/your-username/your-repo-name.git
   ```

2. **Navigate to the project folder**:
   ```bash
   cd extract_name_from_file
   ```

   -> **Update the var with the directory path in your machine**

   ![image](https://github.com/user-attachments/assets/6e1aac62-a8ec-4da4-ae95-9a777890e9bf)

3. **Activate the virtual environment** (if needed):
   - On Windows:
     ```bash
     venv\Scripts\activate
     ```
   - On Linux/macOS:
     ```bash
     source venv/bin/activate
     ```

4. **Run the script**:
   - On Windows:
     ```bash
     python extract_name_file.py
     ```
   - On Linux/macOS:
     ```bash
     python3 extract_name_file.py
     ```

## 📌 Notes

- The algorithm ignores subfolders and only processes files in the specified directory.
- Fully compatible with multiple operating systems.

---

---------------------

# 🗂️ Extract File Names

Este projeto consiste em um simples e eficiente algoritmo em Python que lê os arquivos contidos na pasta `extract_name_from_file/utils/files_to_be_searched` e retorna apenas os nomes desses arquivos **sem a extensão**.

## 💡 Exemplo de Funcionamento

Suponha que a pasta `files_to_be_searched` contenha os seguintes arquivos:

```
doc.pdf
text.txt
```

A saída do programa será:

```
doc
text
```

## 📄 Sobre o Desafio

O desafio original pode ser consultado no arquivo [`chalenge.txt`](extract_name_from_file/chalenge.txt).

## 🛠️ Tecnologias Utilizadas

- Linguagem: **Python**
- Bibliotecas: Apenas **módulos nativos**
- Ambiente Virtual: Configurado com **venv**

## ▶️ Como Executar

1. **Clone o repositório** (caso ainda não tenha):
   ```bash
   git clone https://github.com/seu-usuario/seu-repositorio.git
   ```

2. **Acesse o diretório do projeto**:
   ```bash
   cd extract_name_from_file
   ```
   -> **Coloque o caminho da pasta como está em sua máquina na variável**

   ![image](https://github.com/user-attachments/assets/6e1aac62-a8ec-4da4-ae95-9a777890e9bf)

4. **Ative o ambiente virtual** (se necessário):
   - Windows:
     ```bash
     venv\Scripts\activate
     ```
   - Linux/macOS:
     ```bash
     source venv/bin/activate
     ```

5. **Execute o script**:
   - Windows:
     ```bash
     python extract_name_file.py
     ```
   - Linux/macOS:
     ```bash
     python3 extract_name_file.py
     ```

## 📌 Observações

- O algoritmo ignora subpastas e lê apenas os arquivos no diretório informado.
- O projeto é totalmente compatível com múltiplos sistemas operacionais.
